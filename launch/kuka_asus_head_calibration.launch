<launch>

	<!-- launch the tracker, units in cm -->
	<arg name="marker_size" default="13.2" />
	<arg name="max_new_marker_error" default="0.08" />
	<arg name="max_track_error" default="0.2" />

	<arg name="cam_image_topic" default="/camera/depth_registered/points" />
	<arg name="cam_info_topic" default="/camera/rgb/camera_info" />		
	<arg name="output_frame" default="/camera_link" />

	<node name="ar_track_alvar" pkg="ar_track_alvar" type="individualMarkers" respawn="false" output="screen" args="$(arg marker_size) $(arg max_new_marker_error) $(arg max_track_error) $(arg cam_image_topic) $(arg cam_info_topic) $(arg output_frame)" />

	<!-- launch the rgbd sensor, in this case using openni2 for the asus -->
	<include file="$(find openni2_launch)/launch/openni2.launch">
		<arg name="depth_registration" value="true" />
		<arg name="color_depth_synchronization" value="true" />
	</include>

	<!-- load the default robot configuration -->
	<!--include file="$(find lwr_description)/launch/display.launch">
		<arg name="gui" default="false" />
		<arg name="robot_name" default="lwr_on_box"/>
		<arg name="use_lwr_sim" default="false"/>
		<arg name="load_moveit" default="true"/>
		<arg name="port" default="49948"/>
		<arg name="ip" default="192.168.0.20"/>
	</include-->

	



</launch>